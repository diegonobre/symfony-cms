{% extends '::admin.html.twig' %}
{% form_theme edit_form 'form/bootstrap_3_horizontal_layout.html.twig' %}

{% block body -%}
    <h1>Category edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('cms_category') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
